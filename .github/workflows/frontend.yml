on:
  push:
    branches:
      - live

jobs:
  deploy:
    runs-on: ubuntu-latest    

    env:
      REACT_APP_API_URL: ${{ secrets.REACT_APP_API_URL }}
      REACT_APP_SITE_NAME: ${{ secrets.REACT_APP_SITE_NAME }}
      REACT_APP_LOGO_PATH: ${{ secrets.REACT_APP_LOGO_PATH }}

    steps:
      # Checkout first
      - name: Checkout code
        uses: actions/checkout@v3
      # Tailscale
      - name: Tailscale
        uses: tailscale/github-action@v4
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci 
      - name: Checkout code
        uses: actions/checkout@v3      
        
      # Setup Node
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json
      - name: Install dependencies
        run: npm install --force      
        working-directory: ./frontend
      - name: Build project
        run: CI=false npm run build 
        working-directory: ./frontend

      # Setup SSH
      - name: Set up SSH key
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
      - name: Debug
        run: |
          ping -c 3 172.16.1.50
          ls -al
          find frontend -maxdepth 2

      # Upload build
      - name: Transfer build to server
        run: |
          scp -r \
            -o StrictHostKeyChecking=no \
            -i ~/.ssh/id_ed25519 \
            frontend/ \
            user@${{ secrets.SERVER_IP }}:/var/www/html


      # Deploy on server
      - name: Deploy on server
        run: |
          ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_ed25519 user@${{ secrets.SERVER_IP }} << 'EOF'
            set -e

            echo "Connected to server"

            sudo npm --prefix /var/www/html/frontend run start

            echo "Deployment complete"
          EOF

      # Health check
      - name: Health Check
        run: |
          curl -f https://${{ secrets.HEALTH_CHECK_URL }} \
          || (echo "Health check failed!" && exit 1)

      # Success
      - name: Notify Success
        run: echo "✅ Deployment successful!"
